# P-0002: Jira Tool Enhancements — Depth, Gantt Visuals, Model Epic Creator

**Spec:** [P-0002/spec.md](P-0002/spec.md) | **Created:** 2026-03-19 | **Status:** [IN PROGRESS]

---

## Scope Summary

| # | Scope | Branch | Caps | Status |
|---|-------|--------|------|--------|
| 1 | Fetcher & Report Enhancements | `scope/enhancements` | cap-001, cap-002, cap-003, cap-004 | [DONE] |
| 2 | Create Model Epic | `scope/create-model-epic` | cap-005, cap-006 | [ ] |

---

## Scope 1: Fetcher & Report Enhancements

**Branch:** `scope/enhancements`
**Status:** `[DONE]`
**Depends on:** P-0001 (all core modules must exist)

### Context for Claude Code
- Read `P-0002/spec.md` for full acceptance criteria and the status color palette table.
- Read `P-0002/wireframes.md` for Full Gantt visual separator and color rules.
- All changes are confined to `core/fetcher.py`, `core/excel_builder.py`, and `core/models.py` (depth comment update only).

### Tasks

- [x] **`core/fetcher.py`** — Extend `_fetch_children`: change `if depth >= 2: return` to `if depth >= 3: return`. Update depth comment in `core/models.py` (`Ticket.depth`) to include `3=Sub-task`.
- [x] **`core/excel_builder.py`** — Add `STATUS_COLORS` dict mapping each known status string to a hex color per the palette in `spec.md`. Add a fallback color `EBEBEB` for unknown statuses. Add helper `_status_fill(status) -> PatternFill`.
- [x] **`core/excel_builder.py`** — Update `_sheet_simplified_gantt`: apply `_status_fill(epic.status)` to the Status column cell and to each in-range week cell (replacing the global `GANTT_FILL`).
- [x] **`core/excel_builder.py`** — Update `_sheet_full_gantt`:
  - Apply `_status_fill` to Status column cell and in-range week cells for Epic rows (same as simplified gantt).
  - Sort `epic.children` by numeric ticket ID suffix ascending before iterating.
  - For each child, also iterate `child.children` (sub-tasks) sorted by numeric ID ascending, with 4-space indent in the ID column.
  - Apply thin bottom border (`Side(border_style="thin")`) to every child and sub-task row (all columns including week columns).
  - Apply medium bottom border (`Side(border_style="medium")`) to the last row of each Epic group (all columns).

### Acceptance Criteria
> Copied verbatim from spec.md. Tests must pass against these.

- **cap-001:** Fetcher retrieves tickets at depth 0 (Feature), 1 (Epic), 2 (Story/Task), and 3 (Sub-task). Sub-tasks appear in the Tickets sheet. Comments are fetched for Sub-tasks alongside all other tickets. Depth is capped at 4 levels; no infinite recursion. Features/Epics/Stories with zero children at any level are handled without error.
- **cap-002:** Each distinct status value maps to a unique fill color per the defined palette. Gantt week cells that are filled use the status color of that ticket row, not a single fixed blue. The Status column cell in both Gantt sheets is filled with the same status color. Tickets with an unrecognised status fall back to a neutral gray.
- **cap-003:** In the Full Gantt, a thick bottom border (medium weight) is applied to the last row of each Epic group. A thin bottom border is applied to each child row (Stories/Tasks and Sub-tasks). No blank rows are inserted; separators are purely border-based.
- **cap-004:** In the Full Gantt, the children of each Epic are listed in ascending ticket ID order. Sub-tasks within each Story/Task are listed in ascending ticket ID order. Ticket ID sort is numeric-aware (MTC-9 before MTC-10).

---

## Scope 2: Create Model Epic

**Branch:** `scope/create-model-epic`
**Status:** `[ ]` → `[IN PROGRESS]` → `[DONE]`
**Depends on:** P-0001 Scope 1 (`JiraClient`, `Config` must exist). Does NOT depend on Scope 1 of this project.

### Context for Claude Code
- Read `P-0002/spec.md` sections "cap-005", "cap-006", "flow-01", "Technical Considerations → Jira Creator API", and "Technical Considerations → Template Parsing".
- Read `P-0002/wireframes.md` for the UI layout of the Create Model Epic section.
- Read `P-0002/EpicTemplate.md` to understand the template structure before writing the parser.
- The template file is at `docs/Projects/P-0002/EpicTemplate.md` relative to the repo root.

### Tasks

- [ ] **`core/creator.py`** — New module. Implement `create_model_epic(feature_id: str, epic_name: str, config: Config, client: JiraClient) -> dict`:
  - Parse `docs/Projects/P-0002/EpicTemplate.md` at runtime (read relative to `Path(__file__).parent.parent / "docs/Projects/P-0002/EpicTemplate.md"`).
  - Replace all occurrences of `EpicName` with `epic_name` in each template line.
  - Depth 0 line = Epic row — skip this line; the Epic is created with name `[MODEL] {epic_name}`.
  - Depth 1 lines (4-space indent) = Stories — create with substituted name, parent = Epic.
  - Depth 2 lines (8-space indent) = Sub-tasks — create with substituted name, parent = most recently created Story.
  - Creation order: Epic → for each Story (in template order): create Story → create all its Sub-tasks → next Story.
  - No assignee, due date, start date, or description on any ticket.
  - Return `{"id": epic_key, "url": f"{config.jira_url}/browse/{epic_key}"}`.
- [ ] **`core/creator.py`** — Implement `_create_issue(summary: str, issue_type: str, parent_key: str, config: Config, client: JiraClient) -> str` (returns created ticket key). Use `JiraClient`'s session/auth. For next-gen: set `parent.key`. For classic Epic: use `customfield_10014` for the Feature→Epic link; for Story/Sub-task: use `parent.key`.
- [ ] **`core/jira_client.py`** — Add `create_issue(payload: dict) -> dict` method: `POST /rest/api/3/issue`, raises `JiraAPIError` on non-2xx.
- [ ] **`app.py`** — Add "Create Model Epic" section below the report form:
  - `st.markdown("---")` separator, then `st.subheader("Create Model Epic")`.
  - `epic_name = st.text_input("Epic Name", key="epic_name_input")`.
  - The Feature ID is read from the same `ticket_id` variable already in scope.
  - `st.button("Create Model Epic", disabled=not (ticket_id and epic_name))`.
  - On click: call `create_model_epic` inside a `st.spinner`, show `st.success` or `st.error` inline.
  - Import `create_model_epic` from `core.creator` and `CreatorError` (define in `core/creator.py`).

### Acceptance Criteria
> Copied verbatim from spec.md. Tests must pass against these.

- **cap-005:** A "Create Model Epic" section is visible below the report form in the main area, always rendered regardless of report state. The user enters a Feature ID (text input) and an Epic name (text input). A "Create Model Epic" button is disabled when either field is empty. On success: a green success message shows the created Epic's Jira ticket ID and URL. On failure: an inline error message is shown; no stack trace.
- **cap-006:** The creator reads `docs/Projects/P-0002/EpicTemplate.md` at runtime. Each `EpicName` token is replaced with the user-provided name. The resulting hierarchy: Epic at depth 0, Stories at depth 1, Sub-tasks at depth 2. Tickets are created in order: Epic first, then each Story followed immediately by its Sub-tasks. The Epic is linked to the Feature ID as its parent. Each Story is linked to the Epic. Each Sub-task is linked to its Story. No assignee, due date, start date, or description on any created ticket. The Epic is named `[MODEL] {user_provided_name}`. Stories and Sub-tasks are named exactly as the substituted template line.
